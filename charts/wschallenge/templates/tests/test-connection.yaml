apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "wschallenge.fullname" . }}-test-connection"
  labels:
    {{- include "wschallenge.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "wschallenge.fullname" . }}:{{ .Values.appPort }}']
  restartPolicy: Never
